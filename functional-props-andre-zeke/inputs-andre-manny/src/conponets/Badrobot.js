import React from 'react';

const Badrobot = (props) => {
   
const diffInput = (userinput) => {
            return userinput
          }
  return (
    <>
      <p>{sameInput(props.name)}</p>
    </>
  );
}

export default Badrobot;
